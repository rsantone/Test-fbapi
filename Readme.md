# challenge - Create post, add like, add comment, update it and finally delete it through Graph API

# Install requirements

pip install -r requirements.txt

# Run tests

python -m pytest tests/Test.py
